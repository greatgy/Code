package com.supergenius.xo.finance.service;

import java.util.List;

import com.genius.model.base.entity.Pager;
import com.genius.xo.baseadmin.service.BaseSO;
import com.supergenius.xo.finance.entity.UserStatistics;

/**
 * 用户统计so
 * @author YuYingJie
 */
public interface UserStatisticsSO extends BaseSO<UserStatistics> {

	/**
	 * 获取一个用户统计
	 * @param authoruid
	 * @return
	 * @author YuYingJie
	 */
	UserStatistics obtainOne(String useruid);

	/**
	 * 更新用户状态 
	 * @param uid
	 * @param number
	 * @param field
	 * @return
	 * @author YuYingJie
	 */
	boolean update(String uid, int number, String field);

	/**
	 * 获取被订阅最多的
	 * @param pager
	 * @return
	 * @author YuYingJie
	 */
	List<UserStatistics> getByBeSubscribeCount(Pager pager);

}
