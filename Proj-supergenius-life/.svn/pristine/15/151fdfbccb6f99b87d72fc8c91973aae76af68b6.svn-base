[#ftl]
[#if list??]
	[#list list as topic]
		<li>
			<a href="${base}/topic/${topic.cid}/${topic.oic}" title="${topic.title}">
				<img src="${webimg}/${topic.imgmedium}" alt="${topic.title}" title="${topic.title}">
				<div class="description">
					${topic.title}
				</div>
			</a>
		</li>
	[/#list]
[/#if]

